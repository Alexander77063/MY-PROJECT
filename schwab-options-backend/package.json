{
  "name": "schwab-options-backend",
  "version": "1.0.0",
  "description": "Backend service for Schwab API integration with options scanner",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "logs": "tail -f logs/app-$(date +%Y-%m-%d).log",
    "postinstall": "echo 'Installation complete'"
  },
  "keywords": [
    "schwab",
    "api",
    "options",
    "trading",
    "express",
    "node"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "compression": "^1.7.4",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}